<nav class="navbar navbar-expand-lg bg-dark" >
  <div class="container ">
    <%= link_to "Inicio", root_path, class:"navbar-brand tcolor" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarNavDropdown">
    <ul class="navbar-nav me-auto">
    <li><%= link_to "Noticias", posts_path, class:"nav-link tcolor" %> </li>
    </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle tcolor" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <% if user_signed_in? %>
            <% if current_user.name %>
            Hola <%= current_user.name %>  
            <% else %>
            Hola <%= current_user.email %>
            <% end %>
            <% else %>
            Inicia sesión / Registrate
            <% end %>
          </a>
          <ul class="dropdown-menu">
          <% if user_signed_in? %>
            <li><%= button_to "Cerrar Sesión", destroy_user_session_path, class:"dropdown-item", method: :delete %></li>
            <% else %> 
            <li><%= link_to "Iniciar Sesión", new_user_session_path, class:"dropdown-item" %></li>
            <li><%= link_to "Registrarme", new_user_registration_path, class:"dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>